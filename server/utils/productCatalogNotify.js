/** Notify product consumers when order placement changes stock (optional hook). */
export function notifyProductCatalogChanged() {
  // Frontend listens via product catalog events; server-side no-op for now.
}
